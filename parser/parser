package parser

type Parser Peg {
    Sum int
}

Expr        <- Sum { fmt.Println(text) }
Sum         <- Product (('+' / '-') Product)* {fmt.Println(text)}
Product     <- Value (('*' / '/') Value)*{fmt.Println(text)}
Value       <- '-'?[0-9]+('.'[0-9]+)* /'(' Expr ')'{fmt.Println(text)}

